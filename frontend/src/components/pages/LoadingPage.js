import React from "react";

const LoadingPage = () => (
    <div className="loader">
        <img className="loader__image" src={CDNLink + "/dist/images/loading.gif"} />
    </div>
);

export default LoadingPage;
